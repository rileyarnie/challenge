const initialState = {
  method: "",
  startTime: 0,
  stopTime: 0,
  reportTime: 0,
  startNumber: 0,
  stopNumber: 0,
  totalStarted: [0],
  totalStopped: [0],
  totalNumberRunning: 0,
};

const reducer = (state = initialState, action)=>{

    return state
}

export default reducer